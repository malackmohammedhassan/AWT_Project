# Future Scope

The current project implements URL-based phishing detection only.

Before any future change, check `.github/copilot-instructions.md` first. That file is the active global rule source for this workspace.

Not implemented in this version:

- DOM-based features
- WHOIS features
- TLS/certificate features

These remain intentionally excluded from the active codebase until the project rules are explicitly changed.
